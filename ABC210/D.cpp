#include <bits/stdc++.h>
using namespace std;
int H, W, C, A[1000005];

int main() {
    cin >> H >> W >> C;
    for (int i = 0; i < H; i++) {
        for (int j = 0; j < W; j++) {

        }
    }
    sort(A, A + (H * W));
}