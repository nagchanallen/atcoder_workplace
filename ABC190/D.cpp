#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep0(i, n) for (int i = 0; i < (int)(n); ++i)
#define rep1(i, n) for(int i = 1; i <= int(n); ++i)
using Graph = vector<vector<int>>;
ll MOD = 1e9+7;
ll LLINF = 1e12 + 1;
int INTINF = 2e9;

int main() {
    // ll N, cnt = 0;
    // cin >> N;
    // for (ll i = 1; i <= 2 * N; i++) {
    //     ll lo = -1e12;
    //     ll hi = LLINF;
    //     while (lo + 1 < hi) {
    //         ll mid = (lo + hi) / 2;
    //         if ( ( (2 * (mid - 1) + i - 1) * i / 2) < N ) {
    //             lo = mid;
    //         } else {
    //             hi = mid;
    //         }
    //     }
    //     cout << "lo = " << lo << endl;
    //     if ( ( (2 * lo + i - 1) * i / 2) == N ) cnt++;
    // }
    // cout << cnt << endl;
}