#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep0(i, n) for (int i = 0; i < (int)(n); ++i)
#define rep1(i, n) for(int i = 1; i <= int(n); ++i)
using Graph = vector<vector<int>>;
ll MOD = 1e9+7;
ll LLINF = 3e18;
int INTINF = 2e9;

int N, M, K, A[105], B[105];

int main() {
    // cin >> N >> M;
    // vector<pair<int, int>> reqs;
    // vector<pair<int, int>> balls;
    // rep0(i, M) {
    //     int A, B;
    //     cin >> A >> B;
    //     reqs.push_back(make_pair(A, B));
    // }
    // cin >> K;
    // rep0(i, N) {
    //     int C, D;
    //     cin >> C >> D;
    //     balls.push_back(make_pair(C, D));
    // }
    // sort(reqs.begin(), reqs.end());
    // sort(balls.begin(), balls.end());

    // int ans = 0;
    
    // cout << ans << endl;
}